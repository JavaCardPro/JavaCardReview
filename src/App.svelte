<script>
  import { onMount } from "svelte";
  export let date;

  onMount(async () => {
    const res = await fetch("/api/date");
    const newDate = await res.text();
    date = newDate;
  });
</script>

<main>
  <h1>JavaCard Review</h1>
  <p>{date ? date : 'Loading date...'}</p>
</main>
